# Mobile (React Native / Expo)

This folder is a placeholder for the mobile app. For quick demo, use Expo to scaffold an app:

npx create-expo-app mobile-app
cd mobile-app
npx expo start

You can integrate the generated app with the backend and the .tflite model later.
